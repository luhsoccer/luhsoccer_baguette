---
# Disable all checks at root level so generated items and headers are ignored
# Clang-tidy needs at least one enabled check, so we use the infinite loop check
Checks:              bugprone-infinite-loop
...
